import { IdentityValidation } from './identity.validation';

describe('IdentityValidation', () => {
  it('should create an instance', () => {
    expect(new IdentityValidation()).toBeTruthy();
  });
});
