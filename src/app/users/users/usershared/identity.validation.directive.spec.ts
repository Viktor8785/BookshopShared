import { IdentityValidationDirective } from './identity.validation.directive';

describe('IdentityValidationDirective', () => {
  it('should create an instance', () => {
    const directive = new IdentityValidationDirective();
    expect(directive).toBeTruthy();
  });
});
